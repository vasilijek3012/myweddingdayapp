// Used automatically for `ng build` (production is the default configuration) via
// angular.json's fileReplacements. Fill in your real Railway backend URL once deployed —
// this placeholder will produce failed API calls (CORS/DNS errors) until you do.
export const environment = {
  production: true,
  apiUrl: 'https://YOUR-BACKEND-SERVICE.up.railway.app/api',
};
