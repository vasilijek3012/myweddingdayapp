// Used automatically for `ng build` (production is the default configuration) via
// angular.json's fileReplacements.
export const environment = {
  production: true,
  apiUrl: 'https://myweddingdayapp-production.up.railway.app/api',
};
