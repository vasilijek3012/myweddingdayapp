// Local dev — Angular CLI serves this file by default (ng serve, ng build without
// --configuration production). Swapped for environment.prod.ts on a production build via
// angular.json's fileReplacements.
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',
};
