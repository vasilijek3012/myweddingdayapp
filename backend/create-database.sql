-- Run this once against your PostgreSQL server (e.g. via psql or a GUI client)
-- to create the database before starting the backend.
-- Tables are created automatically by the backend on first start.

-- From a terminal:
--   createdb -U postgres myweddingday
-- or in psql:
--   CREATE DATABASE myweddingday;
